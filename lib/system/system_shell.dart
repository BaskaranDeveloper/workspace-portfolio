import 'package:flutter/material.dart';

class SystemShell extends StatelessWidget {
  const SystemShell({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('System Shell')));
  }
}
