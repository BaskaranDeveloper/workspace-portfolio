import 'package:flutter/material.dart';

class DesktopStatusBar extends StatelessWidget {
  const DesktopStatusBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 24, color: Colors.black);
  }
}
