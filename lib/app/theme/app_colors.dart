import 'package:flutter/material.dart';

class AppColors {
  static const Color backgroundPrimary = Color(0xFF0A0F14);
  static const Color terminalBlue = Color(0xFF00EEFF); // Cyber Blue
  static const Color textPrimary = Color(0xFFEEEEEE);
  static const Color terminalDim = Color(0xFF4A6B70);
  static const Color stateError = Colors.redAccent;
  static const Color background = Color(0xFF0A0F14); // Alias
  static const Color primary = terminalBlue; // Alias
}
